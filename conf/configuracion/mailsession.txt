<mail-session name="Servidor de correo" jndi-name="java:/sae/mail" from="{smtp-user}"> 	
	<smtp-server outbound-socket-binding-ref="sae-mail-smtp" 
	 ssl="true" username="{smtp-user}" password="{smtp-pass}"/> 
</mail-session>
